# PC28 加拿大至尊版 - 宝塔面板 (BT Panel) 极致部署与排错手册

针对您的服务器 `154.12.51.237` 出现的后台 404 及无法访问问题，请**务必严格按照以下步骤重新检查配置**。

---

## 1. 网站设置 (解决 404 的核心)
1. **添加站点**: 域名填 `154.12.51.237`，根目录选择解压后的**项目总目录**。
2. **运行目录设置 (最关键)**:
   - 在宝塔面板 -> 网站 -> 设置 -> **网站目录**。
   - **运行目录** 必须下拉选择 **`/public`**。
   - 点击保存。
3. **关闭防跨站攻击**:
   - 在同一个页面，**取消勾选** “防跨站攻击 (open_basedir)”。
   - **为什么要关？** 因为核心代码在 `/src` 目录，不关闭则 PHP 无法读取数据库配置。

## 2. 数据库配置
1. **导入 SQL**: 在宝塔数据库菜单中，将 `database/mysql_schema.sql` 导入您的数据库。
2. **修改配置文件**: 使用宝塔文件管理器打开 **`src/Config/database.php`**，填入正确的数据库名、用户名和密码。

## 3. 访问与环境检查 (新功能)
为了帮您定位问题，我为您编写了一个自检工具：
- **自检地址**: `http://154.12.51.237/check_env.php`
- 打开这个地址，它会告诉你：
    - 数据库连上没？
    - 防跨站攻击关掉没？
    - 核心文件缺失没？

## 4. 后台管理入口
- **后台地址**: `http://154.12.51.237/admin/index.php`
- **默认账号**: `admin`
- **默认密码**: `admin123`
- **无法登录？** 如果忘记密码，在宝塔终端执行：`php /www/wwwroot/您的目录/scripts/reset_admin.php`。

## 5. 采集系统 (深度28 专用)
1. 确保服务器能访问外网。
2. 在宝塔 **计划任务** 添加：
   - 任务名: `PC28采集`
   - 周期: 每 1 分钟
   - 脚本: `php /www/wwwroot/您的目录/scripts/scraper.php`

## 6. 常见问题排查
- **访问显示 403**: 检查文件夹权限，建议将项目目录设为 `755`，所有者设为 `www`。
- **访问后台 404**:
    - 检查“运行目录”是否设为 `/public`。
    - 检查 `/public/admin/index.php` 文件是否存在。
- **采集没数据**: 检查数据库 `lottery_results` 表是否有记录。

---
**提示**: 部署成功后，出于安全考虑，请在验证通过后删除 `public/check_env.php` 文件。
