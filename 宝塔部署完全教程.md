# PC28 加拿大至尊版 - 宝塔面板 (BT Panel) 部署完全教程

本教程旨在指导您在宝塔面板上从零开始搭建并运行 PC28 平台，包含 Bug 修复后的最新配置。

---

## 1. 环境准备
在宝塔面板的“软件商店”中安装以下环境：
- **Web 服务器**: Nginx 1.18+ 或 Apache 2.4
- **PHP 版本**: PHP 7.2 或 PHP 7.4 (必须安装 `curl`, `pdo`, `pdo_mysql` 扩展)
- **数据库**: MySQL 5.6 (内存 2G 以上建议 5.7)

## 2. 网站与数据库创建
1. **添加站点**:
   - 域名: 输入您的域名或 IP。
   - 根目录: 选择解压后的项目根目录。
   - **运行目录**: 极其重要！在站点设置 -> 网站目录中，将“运行目录”设置为 `/public`。
2. **创建数据库**:
   - 在宝塔“数据库”菜单中创建一个新数据库，并记录数据库名、用户名和密码。
   - 点击“管理”或“导入”，上传并执行项目根目录下的 `database/mysql_schema.sql`。

## 3. 配置文件修改
使用宝塔文件管理器，找到 `src/Config/database.php`：
```php
return [
    'host' => '127.0.0.1',
    'dbname' => '您的数据库名',
    'user' => '您的数据库用户名',
    'password' => '您的数据库密码',
    'port' => 3306,
    'driver' => 'mysql'
];
```

## 4. 解决后台登录与权限问题
1. **防跨站攻击 (open_basedir)**:
   - 进入站点设置 -> 网站目录，**取消勾选**“防跨站攻击 (open_basedir)”，否则 PHP 无法访问 `src/` 目录。
2. **重置管理员**:
   - 在终端执行：`php /www/wwwroot/您的目录/scripts/reset_admin.php`。
   - 默认账号：`admin` 密码：`admin123`。
3. **SSL 证书**:
   - 建议在站点设置 -> SSL 中开启 Let's Encrypt 免费证书，保证数据传输安全。

## 5. 配置自动采集与结算 (计划任务)
在宝塔“计划任务”菜单中添加以下三项，任务类型选择“Shell 脚本”：

### (1) 自动采集开奖 (建议每 1-2 分钟)
- 任务名称: `PC28 采集`
- 脚本内容: `php /www/wwwroot/您的目录/scripts/scraper.php`
- 执行周期: N 分钟 (1)

### (2) 自动结算注单 (建议每 1 分钟)
- 任务名称: `PC28 结算`
- 脚本内容: `php /www/wwwroot/您的目录/scripts/settle.php`
- 执行周期: 每分钟

### (3) 模拟机器人投注 (可选，建议每 1 分钟)
- 任务名称: `PC28 机器人`
- 脚本内容: `php /www/wwwroot/scripts/bot_betting.php`
- 执行周期: 每分钟

## 6. 常见 Bug 修复说明
- **后台无法访问**: 确认 URL 为 `域名/admin/index.php`，且已通过 `scripts/reset_admin.php` 初始化账号。
- **采集失败**:
  - 检查服务器是否能访问外网。
  - `scraper.php` 已内置模拟逻辑，即使源站失效，也会生成模拟数据保证游戏继续。
- **页面 404**: 确认 Nginx 的伪静态配置为空或默认，且运行目录正确指向 `/public`。

---
**提示**: 部署完成后，请第一时间登录后台修改管理员密码，并检查 `/recharge.html` 中的收款二维码是否已替换为您自己的。

## 7. 常见报错排查 (Troubleshooting)

### (1) 登录/注册提示“服务器异常”
- **原因**: 数据库连接失败或 `src/Config/database.php` 配置错误。
- **排查**:
  - 检查数据库服务是否开启。
  - 检查配置文件中的 `host` (尝试 `127.0.0.1` 而不是 `localhost`)。
  - 检查数据库用户名和密码是否正确。

### (2) 页面没有响应 (No Response)
- **原因**: 运行目录未指向 `/public`，或者 PHP 报错被关闭。
- **排查**: 确认宝塔站点设置中的“运行目录”已设为 `/public`。

### (3) MySQL 5.6 导入 SQL 报错
- **原因**: 使用了较新版本的 SQL 语法 (如 `IF NOT EXISTS` 在 `ALTER TABLE` 中)。
- **解决**: 使用本项目优化后的 `database/update_odds_rules.sql`，或者手动检查字段是否存在。

### (4) 机器人不自动投注
- **原因**: 计划任务未配置或 `scripts/bot_betting.php` 执行超时。
- **解决**: 在宝塔计划任务中将周期缩短至 1 分钟，并手动在终端运行测试：`php /www/wwwroot/您的目录/scripts/bot_betting.php`。
